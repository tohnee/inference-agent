# Historical implementation plans

These documents are historical implementation plans. They explain why earlier repository changes were made, but the current user-facing workflow is documented in the root `README.md` and `auto-profiling/README.md`.
